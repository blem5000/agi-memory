# GitHub reads this file to render the "Sponsor" button on the repository page.
#
# Nothing is enabled yet on purpose. A funding entry pointing at an account that
# is not set up renders a button that goes nowhere, which is worse than no
# button. Uncomment a line once that account actually exists.
#
#   github          Requires enrolling at https://github.com/sponsors/accounts
#                   (identity + bank/Stripe + tax details, approval is usually
#                   same-day). Checked 2026-09-14: not yet enrolled.
#   buy_me_a_coffee No approval needed, live in minutes. One-off tips.
#   ko_fi           No approval needed, lower fees, one-off and monthly.
#   custom          Any URL you control -- Polar, a Stripe payment link, PayPal.
#
# Up to four entries render; GitHub shows them in the order listed here.

# github: kdbhalala
# buy_me_a_coffee: YOUR_USERNAME
# ko_fi: YOUR_USERNAME
# custom: ["https://example.com/support"]
